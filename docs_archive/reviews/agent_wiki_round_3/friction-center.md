# friction.md - Mr. Center (seat B), run 3 (2026-09-06)

Mark: [NEW] = files/pictures feature of this run, [R2] = already reported in round 2.

## Log

14:32:21Z start, tool list read (336 lines, 64839 bytes), status ok: name=Second Wiki Test member=Mr. Center threshold=2 chain_governed=true chain_diverged=[]
14:33Z [R2] `list_proposals` on this node lists ONLY this seat's own proposals (all 36 rows `by: Mr. Center`, plus genesis). The other seats' proposals are invisible here - the "stale queue" of round 2 could only be read for my own two (25, 55).
14:33Z [R2] `read_proposal 25` on my OWN open proposal reports `approvals: 0` and my own vote as `open`, although the tool text says "the proposer's own signature is the first of m". Two different truths in two places.
14:35Z [R2] `withdraw` answers a bare `{"reply":"ack"}` - no state, no id, no confirmation which proposal it hit. approve/decline return the full record; withdraw does not.
14:34Z HARNESS (not MCP): `./mcp <tool> @-` (stdin) is not supported - only `@<file>`. Long chat bodies need a temp file.
14:41Z [NEW] share_file -> hash: five files (1.5 KB .. 105 KB) shared at 14:41:02/03, all five carried their 64-hex `checksum` in read_uploads at 14:41:07. Under 5 s, no polling loop needed in practice.
14:41Z [NEW] read_uploads fields the tool text does not explain: `availability: "sharer-only"`, `mirror_held`/`mirror_of` (piece counts, e.g. 3/3 for a 102 KB PNG, 1/1 for a 1.5 KB CSV) and `mirrors: 1`. Guessed meaning: pieces held of pieces total, and the number of declared mirrors. `expires_ts` is set (7-day chat window) while `persistent: false`.
14:41Z [NEW] `propose {surface:"files", payload:{op:"persist", id}}` took the SHARE MESSAGE id (32-hex), as documented, and answered `proposed` with its own patch channel. Five in a row, ids 130/133/136/139/142.
14:42Z [NEW] EXPERIMENT dangling reference: `wiki_edit` dry_run with `upload:0123456789ab` answers exactly one warning - `file reference unresolved: upload:0123456789ab` - and (without allow_warnings) REFUSES the call. Good: the fault is named, not swallowed.
14:42Z [NEW] EXPERIMENT reference before persist: with the persist vote still pending, `resolve_upload 2db745a887c7` reads `temporary: true, local.kind: own`, and `wiki_edit` proposes WITHOUT any warning (proposal 145). Documented behaviour ("a match that is only TEMPORARY does not warn"), but it means the ONLY guard against a page pointing at an expiring file is the author's discipline - or wiki_health.files.temporary afterwards.
14:46Z CORRECTION to the 14:33 entry: list_proposals DOES show other seats' proposals - as soon as they have one open. What it does NOT show is other seats' ALREADY APPLIED proposals from earlier rounds (round 2 left 36 rows, every one of them mine). So the local proposal history is own-seat-only; the live queue is complete.
14:46Z [R2] The proposer's own signature: a FRESHLY filed proposal reads approvals 1 with my own vote "approved" - correct. My two round-2 proposals (25, 55) read approvals 0 with my own vote "open": the proposer's signature did not survive the restart/checkpoint between the rounds. Same tool, two answers, and only the second one is documented.
14:47Z [NEW] read_chain blocks carry NO timestamp (ts: None on every block), and read_state {surface: files} carries only shared_ts. "Seconds from share to persisted" is therefore not measurable from the API - it has to come from the caller's own clock around the calls. Logged mine by hand.
14:48Z [NEW] MIRROR, measured on this node: Left's 87814-B PNG, persist applied ~14:43:2x -> 14:43:35 local.kind "partial" (held 0 of 2, holders [Mr. Left]) -> 14:48:22 "mirrored" (2 of 2, all three holders). Right's two files were "mirrored" at first sight. Left's 145459-B file: 14:58:37 "partial" 3 of 4 -> 15:03 "mirrored". So the trickle needs minutes, not seconds, and its unit is PIECES: `mirror_held/mirror_of` in read_uploads and `held/of` in read_mirror both count pieces, not bytes (2 pieces for 88 KB, 3 for 102 KB, 4 for 145 KB).
14:49Z [NEW] `availability` takes at least two values that the tool text never explains: "sharer-only" while nobody else holds the bytes, "relay-held" once the pieces are published for the mirror. Neither word appears in read_uploads' description.
15:05Z [NEW] `wiki_health` refused once with "the wiki index is still building (0 of 184 documents)" right after a 5-page proposal applied. A retry seconds later worked. Not an error in the tool - but a caller that treats health as always-available gets a hard error, and the message is the only place this state is named.
15:06Z [NEW] A file reference to a NOT-YET-PERSISTED file passes wiki_edit silently (measured twice: my 145, Mr. Right's 189). The only guard is wiki_health.files.temporary AFTER the fact. A warning at edit time ("resolves only to a temporary share") would cost nothing and would have caught both cases.
15:12Z [R2-adjacent, sharpened] wiki_edit's alias/title warning is computed against the BASE, not against the result of the edit sequence: a rename that MOVES an alias from the old page to another page in the SAME call is refused with "alias X already names <old path> - both pages lose it", although the call removes it there. Workaround: dry_run + with_patch to verify the end state, then allow_warnings: true. A caller who trusts the warning would never move an alias.
15:12Z HARNESS (mine, not the tool): a heredoc-written args file lost its closing brace and moltmcp died with a JSONDecodeError pointing at line 2 - the wrapper never validates the file, so a truncated args file looks like a protocol error. Building the args with python's json.dump instead of a heredoc removes the class of error.
15:20Z [R2] No tool in this wiki detects a relation asserted in the WRONG DIRECTION. wiki_health has no check for it, wiki_edit does not warn, and the prose reads correctly either way. Measured over all 197 pages: 13 reversed authored_by edges, every one of them on my own seat's person pages from round 2 - found only because I went looking for the same defect in someone else's proposal. A `wiki_health` group "predicate asserted on a page whose type contradicts it" would have found all 13 in one call.
15:17Z [NEW] A wiki proposal can go `superseded: true` WITHOUT any other proposal touching its paths: the folded base itself moved (wiki_changes.base jumped from 28d28805... to 2334dce1..., wiki_rev restarted at small numbers) and my 4-path patch stopped applying. Two traps in one: (a) the proposal STAYS in state "proposed" and looks votable - only read_proposal carries the `superseded` field, list_proposals does not; (b) the PROPOSER's own signature is dropped by it (205 read approvals 1 = the other seat's, approved_by_me false, my own vote "open" on my own proposal). A caller counting votes waits forever. Remedy: wiki_edit {..., supersedes: <id>} re-proposes and withdraws in one step.
15:18Z [NEW] `wiki_edit` refuses on WARNINGS by default, and "path is in open proposal N" is such a warning - including when proposal N is the caller's OWN dead (superseded) proposal. Re-proposing then needs allow_warnings: true, i.e. the correct recovery path is only reachable by overriding a warning.
15:19Z [NEW] MEASURED HAZARD: replacing a shared file's bytes on disk after the share is invisible to the node. I overwrote three already-PERSISTED files in the exchange folder; resolve_upload still answered temporary:false, available:true, local.kind own and the OLD size for all three. Nothing re-hashes, nothing warns. A share must be treated as immutable from the moment share_file runs - a new version needs a new file name. (The mirrors at the other seats still hold the original bytes, so the persisted content survived my mistake.)
15:20Z [NEW] `unpersist` is undocumented in the tool text beyond a half sentence in read_uploads, and it needs a field nobody names: `propose {surface:"files", payload:{op:"unpersist", id}}` fails with `bad payload: unpersist needs its stamp \`at\``. Guessed and confirmed: `at` is the share's `ts` from read_uploads. Two calls of guessing for an operation the tool text calls "a vote that moves it back".
15:24Z [NEW, INCIDENT] DIVERGENZ confirmed. Mr. Right's node stopped at height 96; mine ran to 115. My node's block 97 IS the compaction checkpoint and its signers are ['Mr. Center', 'Mr. Right'] - his node SIGNED the cut and then rejected the sealed block ("the shared memory base this node holds is not the committed one"). Every block of mine from 98 on is signed ['Mr. Center', 'Mr. Left']; his signature never appears again.
15:24Z [NEW, INCIDENT] Both built-in detectors stayed silent through all of it: read_chain.diverged = [] and status.chain_diverged = [] on my node at 19 blocks of distance, and the same on his. The three symptoms that actually revealed it: (1) five open proposals of three seats jumped to `superseded` with no shared path, (2) the chat's system lines (⚖ #N ✓) reported decisions his node did not have, (3) his head stood still while proposals were being answered.
15:24Z [NEW] A checkpoint re-folds the base and kills EVERY open patch computed against the old base (`wiki_changes.base` changes, `wiki_rev` restarts). That is the mechanism behind the superseded wave - not a path conflict.
15:26Z [NEW] MIRROR TIMINGS, measured on this node (poll every 20 s), piece cadence matches settings.mirror_publish_interval_secs = 15:
  left-at-protocol-weg.png   145 KB / 4 pieces: persistent 14:56:14 -> partial 14:57:36 -> mirrored 14:58:57 (2:43)
  left-nostr-kind-baender.png 104 KB / 3 pieces: persistent 14:55:54 -> mirrored 14:57:36 (1:42)
  mr-right-abloeseketten.png  88 KB / 3 pieces: persistent 14:49:06 -> 1/3 14:49:26 -> 2/3 14:49:46 -> mirrored 14:50:07 (1:01)
  left-nostr-verschluesselungslinie.png 88 KB / 2 pieces: partial 14:43:35 -> mirrored 14:48:22
  `holders` grows from [sharer] to all three seats exactly when the last piece lands.
15:26Z [NEW] `unpersist` verified end to end: after proposal 229 sealed, read_uploads reads persistent:false and a FRESH expires_ts (the 7-day chat window) on that share - the bytes stay, the pin goes.
15:26Z [NEW] A proposal can be `superseded: true` AND still apply (238, 244 both sealed with the flag set). So the flag means "the base moved under it", not "it is dead" - two of mine died of it (205), two survived it. Nothing in the tool text distinguishes the two cases.
15:26Z WHAT WORKED WITHOUT FRICTION, so the list is not only complaints: share_file -> checksum in under 5 s for five files at once; propose/approve/decline with `note` (the reason lands in the patch channel before the vote, every time); wiki_edit dry_run + with_patch as a review instrument (it caught two of my own reversed edges before they landed); wiki_resolve, which stopped two duplicate pages ("MSC4186", "Element X" already bound as aliases); wiki_health as the one call that answers "what next"; and the file grammar itself - `![alt](upload:<12 hex>)` resolved on the first attempt on every page, and wiki_get.files reported name + state per reference exactly as documented.

## Closing state (15:33Z)

- chain head 124 (proposal 260), read_chain.diverged = [], status.chain_diverged = [] - while one seat's node stands at 96. The detectors named by the run's rules never fired.
- wiki: 212 documents (125 at the start), wiki_rev 17, base 2334dce161b1...
- wiki_health: dangling 0, key_drift 0, files 0/0/0. Orphans 5, one of them mine (ereignisse/threads-foederation.md) - held back because rule 2 (DIVERGENZ -> stop proposing) came in before I could file the one-line link.
- my five files: all persistent, all `local.kind: own`, referenced from 2 pages, `wiki_get.files` reports name + state for each.

## What round 2 already reported and what is NEW here

R2 (unchanged): own-seat-only proposal history; the proposer's signature not being visible on old proposals; withdraw's bare ack; no detector for a relation asserted in the wrong direction.
NEW in this run (files/pictures): share->hash timing, availability words, mirror piece mechanics and timings, the missing timestamps on chain blocks, the silent pass of a not-yet-persisted reference, the undocumented `at` field of unpersist, the base-fold superseded wave and the lost proposer signature, and the measured hazard of replacing a shared file's bytes on disk.
